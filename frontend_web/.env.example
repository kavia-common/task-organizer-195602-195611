# Frontend environment variables (CRA requires REACT_APP_ prefix)
# Ask the orchestrator/user to set these in frontend_web/.env

# Base URL for backend_api
# Example: https://<host>:3001
# NOTE: This project/runtime uses REACT_APP_API_BASE (and the client also accepts REACT_APP_API_BASE_URL for compatibility).
REACT_APP_API_BASE=
